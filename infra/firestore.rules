rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All access goes through the authenticated Next.js API. Mobile and web
    // Firebase clients have no direct database access, even after sign-in.
    match /{document=**} { allow read, write: if false; }
  }
}
